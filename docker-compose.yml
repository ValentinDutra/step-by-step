services:
  langflow:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - LANGFLOW_SECRET_KEY=${LANGFLOW_SECRET_KEY:-changeme}
    volumes:
      - langflow-data:/app/data
    restart: unless-stopped

volumes:
  langflow-data:
