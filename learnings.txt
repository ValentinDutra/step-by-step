# Ralph learnings — Codex & Gemini per-phase providers (#16)

- Runner: `uv run python ...` and `uv run pytest`. Python 3.13.11, uv 0.8.15.
- Gemini headless needs BOTH `GEMINI_API_KEY` (set in ~/.zshrc) AND `GEMINI_CLI_TRUST_WORKSPACE=true` (or `--skip-trust`), else it refuses in untrusted dirs. Verified gemini 0.46.0.
- `gemini --output-format json` returns ONE buffered object `{session_id, response, stats}` (not JSONL). Parse `.response`.
- codex 0.139.0 authed via ~/.codex/auth.json. Prompt via stdin uses `codex exec - --json`.
- Bash calls print a harmless zoxide config warning; ignore it.
- CODEX BLOCKER: `codex exec` rejects EVERY model (gpt-5.2/gpt-5.1-codex/gpt-5-codex/gpt-5/...) with 400 "model not supported when using Codex with a ChatGPT account". The user's ChatGPT account has no codex-exec (headless/API) entitlement. Needs OPENAI_API_KEY or a plan with Codex API access to run for real.
- Codex fixtures: codex_error.jsonl is a REAL capture (verified error path). codex.jsonl SUCCESS is DOC-DERIVED from takopi.dev exec-json cheatsheet + OpenAI docs (item.completed item.type=agent_message -> item.text; turn.completed has usage tokens, no USD). VERIFY success path once codex exec auth works.
- Codex error envelope: {"type":"error","message":"<json-string>"} and {"type":"turn.failed","error":{"message":"<json-string>"}}; message is itself JSON -> unwrap to .error.message (see _extract_codex_error).
- The Bash-tool shell does NOT source ~/.zshrc, so GEMINI_API_KEY (added to ~/.zshrc) is NOT visible here — must pass inline for testing. The user's real `pipeline` runs from interactive zsh which DOES source it, so the gemini subprocess inherits the key. Fine in production.
- Gemini passes prompt via `-p <prompt>` (NOT stdin). GeminiProvider sets GEMINI_CLI_TRUST_WORKSPACE=true in subprocess env. json output is buffered {session_id,response,stats} -> parse .response. VERIFIED end-to-end: GeminiProvider().run() returned success/output='ok'/cost=None and streamed once.
- Gemini default model is gemini-3.5-flash (router uses gemini-3.1-flash-lite). Model names are 3.x now; config examples using gemini-2.5-pro are stale but pass-through so harmless.
- preflight() was ALREADY added to app/config.py in Task 6. Task 8 only needs to write its test + wire it into runner.py (don't redefine it).
- Config tests must monkeypatch XDG_CONFIG_HOME or they'd read the real ~/.config/step-by-step/config.toml.
- PLAN GAP: app/git.py ALSO imported call_claude (not listed in the plan). Two uses: run_commit_pr_stage (has `stage` -> uses stage.provider) and create_branch (NO stage -> injected `provider` param). create_branch caller in runner.py:66 passes stage_map["Commit & PR"][1].provider. Handled during 9b.
- evaluate gate: provider.run already records cost, so evaluate does NOT add_call separately. Callers (runner.py, runner_steps.py) pass ClaudeProvider() interim; task 11b swaps to self._default_provider.
- Dataclass/Protocol style lives in app/models.py; providers in app/providers/.
- pytest ALREADY set up in Task 2: dev group `pytest>=9.0.3` + `[tool.pytest.ini_options] pythonpath=["."]`. Task 3 should skip "add pytest", just verify.
- Float cost asserts: use `pytest.approx` (0.30+0.12 != 0.42 exactly); but `:.4f` formatting rounds fine for format_cost tests.
